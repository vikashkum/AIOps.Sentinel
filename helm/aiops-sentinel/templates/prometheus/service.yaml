{{- if .Values.prometheus.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "aiops-sentinel.prometheus.name" . }}
  namespace: {{ include "aiops-sentinel.namespace" . }}
  labels:
    {{- include "aiops-sentinel.labels" . | nindent 4 }}
    app.kubernetes.io/component: prometheus
spec:
  type: {{ .Values.prometheus.service.type }}
  selector:
    {{- include "aiops-sentinel.componentLabels" (dict "component" "prometheus" "ctx" .) | nindent 4 }}
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.prometheus.service.port | default .Values.prometheus.port }}
      targetPort: http
      {{- if or (eq .Values.prometheus.service.type "NodePort") (eq .Values.prometheus.service.type "LoadBalancer") }}
      nodePort: {{ .Values.prometheus.service.nodePort }}
      {{- end }}
{{- end }}
