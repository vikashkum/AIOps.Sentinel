apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "aiops-sentinel.frontend.name" . }}-nginx
  namespace: {{ include "aiops-sentinel.namespace" . }}
  labels:
    {{- include "aiops-sentinel.labels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
data:
  # The nginx.conf baked into the Docker image hardcodes "backend" as the
  # upstream hostname. In Kubernetes, the backend Service is named
  # "{{ include "aiops-sentinel.backend.name" . }}" (e.g. "aiops-backend").
  # We mount this ConfigMap over the image's default to inject the correct name.
  default.conf: |
    server {
        listen 80;
        root /usr/share/nginx/html;
        index index.html;

        location / {
            try_files $uri $uri/ /index.html;
        }

        location /api/ {
            proxy_pass http://{{ include "aiops-sentinel.backend.name" . }}:{{ .Values.backend.port }}/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }
    }
